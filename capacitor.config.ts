import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pexip.pexgo',
  appName: 'PexGO',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
